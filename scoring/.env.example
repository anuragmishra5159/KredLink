# KredLink Python Scoring Microservice — Environment Configuration
# Copy this file to .env or configure these variables in Render environment settings

# Port to run the Flask microservice locally (defaults to 5001)
SCORING_PORT=5001

# The production port (Render assigns this dynamically in production via $PORT)
# PORT=10000
