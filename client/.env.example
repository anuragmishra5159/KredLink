# KredLink Frontend — Environment Configuration
# Copy this file to .env or .env.local to configure locally

# The URL of the Express backend API (v1 prefix included)
# In production, set this to the deployed URL (e.g. https://kredlink-backend.onrender.com/api/v1)
VITE_API_URL=http://localhost:5000/api/v1
